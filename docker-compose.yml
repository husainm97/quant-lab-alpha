services:
  quant-lab:
    build: .
    container_name: quant-lab-alpha
    volumes:
      - .:/app
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    environment:
      - PYTHONUNBUFFERED=1
      - DISPLAY=host.docker.internal:0
    network_mode: host
    command: python main.py